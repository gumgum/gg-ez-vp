export default class GGezvp {
  constructor(setup) {
    this.config = {
      container,
      width,
      height,
      src,
      controls,
      autoPlay,
      volume,
      mute,
      poster,
      preload,
      loop
    } = setup;
  }
}
